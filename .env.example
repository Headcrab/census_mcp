# Пример файла .env для настройки census-mcp
# Скопируйте этот файл в .env и заполните значениями для вашей среды

# Обязательная переменная для работы с Census API
# Получить ключ API можно на сайте: https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=ваш_ключ_api

# Режим работы сервера (stdio или sse)
# По умолчанию: stdio
# TRANSPORT=stdio

# Порт для HTTP сервера (используется только в режиме SSE)
# По умолчанию: 8080
PORT=8080

# Тестовый режим (true или false)
# В тестовом режиме используется мок-клиент Census API
# По умолчанию: false
TEST_MODE=false

# Уровень логирования (debug, info, warn, error)
# По умолчанию: info
LOG_LEVEL=debug

# Путь к файлу для сохранения логов
# По умолчанию: логи выводятся в stdout
LOG_FILE=./logs/census-mcp.log 